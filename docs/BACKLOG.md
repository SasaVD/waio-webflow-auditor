# Product Backlog

Persistent product-level items that need to survive across conversations and checkpoints. Not for action — for memory. Move items out when they become scoped work.

---

## Open

- **Workstream E — Audit-scoped shared browser context.** Plan ready for execution: [2026-04-27-workstream-e-accessibility-shared-context.md](superpowers/plans/2026-04-27-workstream-e-accessibility-shared-context.md). Single-commit refactor (~3 files). Closes the accessibility-Playwright addendum: `accessibility_auditor.run_accessibility_audit` launches its own Playwright instance independently of the homepage `fetch_page`, so bot protection can challenge the second instance even when the first succeeded (observed on sched.com 04-26 audit `0e2b5690-5de5-4228-90a5-b8c376991aa9`). Approach A+C combo (shared `BrowserContext` per audit + reuse homepage soup where possible). No external dependencies, no cost. Filed 2026-04-27.

- **Workstream F — Dual-source DFS homepage analysis.** Scoping stub: [2026-04-27-workstream-f-dual-source-dfs.md](superpowers/plans/2026-04-27-workstream-f-dual-source-dfs.md). When D1 detects bot-challenge but DataForSEO has the homepage, use DFS-rendered HTML as authoritative source for the 10 pillars (currently the homepage HTML sitting in DFS's response is discarded). Approach B locked: Playwright first, DFS Instant Pages on bot-challenge. Open questions remain (DFS Instant Pages WAF coverage, cost guardrail, UX framing). Pre-execution requires Workstream E shipped + investigation spike on DFS Instant Pages WAF behavior. Original Workstream E goal as filed 2026-04-23 — renamed F at 2026-04-27 scoping pass.

- **Workstream G — DataForSEO crawl reliability investigation.** Scoping stub: [2026-04-27-workstream-g-dfs-reliability.md](superpowers/plans/2026-04-27-workstream-g-dfs-reliability.md). Three production audits during D verification produced trivial DFS crawls (Ticketmaster, cvent, sched.com partial). Phase 1 is an investigation spike: contact DataForSEO support, get residential-proxy pricing, run a controlled test on one bot-protected site. ~1 week scoped. Phase 2 implementation only follows if Phase 1 returns favorable cost/improvement numbers. Pre-execution requires Workstream F shipped (F's dual-source approach may close most of the gap G is trying to address). Filed 2026-04-27.

- **Telemetry observability PR (small, standalone).** Two-line counter increments at the existing log paths: `bot_challenge.detected` (in `main.py:perform_audit` and `perform_premium_audit`), `crawl_status="no_data"` (in `_enrich_report_from_crawl`'s trivial-crawl branch), `accessibility scan_status="failed"` (in the accessibility auditor's exception path). Surface aggregates via a small admin endpoint (e.g. `GET /api/admin/bot-protection-stats` — auth-gated). Not gated on E/F/G; can land any time. One-day scope. Surfaces the data needed to size F's WAF-coverage decision and G's cost ceiling. Filed 2026-04-27.

- **Workstream C — Homepage-only premium audit ceiling.**

- **Workstream C — Homepage-only premium audit ceiling.** Premium audits currently run the 10 deterministic pillars only on the homepage; DataForSEO supplies link graph + per-page metadata but no per-page pillar findings. All drill-down (Workstream B, Phase 2) is therefore bounded to homepage data regardless of crawl size. Resolving this has cost, architecture, and positioning implications (auto per-page pillar runs at 2,000-page scale vs opt-in per-URL, vs tiered "full drill-down" upsell). Deferred — deserves a dedicated scoping pass after Phase 1.5 (Workstream A quick wins) and Phase 2 (Workstream B drill-down UX) ship. Raised in the 2026-04-22 URL-level data model audit; documented 2026-04-23. — Eda's feedback.

- **Migrate frontend off `detected_industry` legacy field.** Backend `ai_visibility/engine.py:324` carries a `# TODO: remove after frontend fully migrated to industry.value`. Verified during sweep #7 of 2026-04-28 polish sprint: 7+ frontend consumers still read `detected_industry` directly (generateMarkdown.ts:153, generateExcel.ts:174, DashboardOverviewPage.tsx:179, DashboardSummaryPage.tsx:47, DashboardContentIntelligencePage.tsx:85+205+206+350, AIVisibilityModal.tsx:126). Plus the aiVisibilityStore.ts type still has both fields. Migration order: switch each consumer to read `industry.value` (with fallback to `detected_industry` for old reports), verify across recent audits, then remove the legacy field on backend. Sizing: M (~half day for the per-consumer migration + one full-stack regression sweep). Severity: Low — both fields currently present, no user impact. Filed 2026-04-28.

- **Additional user-visible 'DataForSEO' jargon leaks.** Sweep #3+#5 (commit f2096e0) fixed the two most damaging DashboardLayout + DashboardGraphPage leaks. Grep surfaced 7 more user-visible DataForSEO mentions: DashboardClustersPage:1042, DashboardSummaryPage:133, DashboardPagesPage:296+318+617, DashboardLinkIntelligencePage:847+857, plus DashboardAIVisibilityPage:502+503 (the AI Visibility methodology section legitimately credits DataForSEO's pre-indexed LLM corpus — that one is appropriate). Triage: replace bot-protection / crawl-failure copy that mentions DataForSEO with vendor-neutral phrasing; preserve methodology credits where vendor attribution is part of the value-prop. Sizing: S (~1 hour). Severity: Low (most are in feature-unavailable empty states, less prominent than the two fixed in sweep #3+#5). Filed 2026-04-28.

- **Frontend test infrastructure (vitest + @testing-library/react).** Backend has 221 pytest tests; frontend has zero. Verification surface today is `tsc -b && vite build` plus manual smoke after Railway deploy. Day 2 of the polish sprint (drill-down UX, commit `c8fa355`) shipped without component tests because standing up the framework was scope creep against the SEO-review timeline. Add post-sprint: vitest + @testing-library/react + a small starter suite covering FindingElementsList edge cases (empty array → returns null, hidden-count overflow message, missing selector/snippet/location fields rendered partially). Sizing: ~1 day. Severity: Med — the dashboard-rendering surface keeps growing; manual smoke alone won't scale. Filed 2026-04-28.

- **Subagent permission story — Bash + outbound network access for background agents.** Hit during the 2026-04-27 polish sprint Wave 1 dispatch: 3 of 4 background subagents (general-purpose tool group) failed because they couldn't get interactive approval for Bash calls (network, grep, git stage/commit). Foreground Bash works because the prompt round-trips to the user's client; background agents have no human-in-the-loop and stall at 600s no-progress watchdogs. Workaround used: ran the failed buckets foreground in the orchestrating session — costs orchestrator context but ships work. Permanent fix: probably a `.claude/settings.json` allowlist for safe Bash commands (read-only `git status`, `git diff`, `grep`, `find`) plus an explicit allowlist for outbound HTTPS to known API hosts (api.dataforseo.com etc.). Filed as not-urgent; address after the polish sprint closes. The `update-config` skill is the relevant tool. Filed 2026-04-27.
